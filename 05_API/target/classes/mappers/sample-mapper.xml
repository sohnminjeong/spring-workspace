<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sampleMapper">
	
	<insert id="insertSample" parameterType="Sample">
		INSERT sample (name, addr, outline, use_hour, start_date, end_date, detail)
		VALUES(#{name}, #{addr}, #{outline}, #{useHour}, #{startDate}, #{endDate}, #{detail});
	</insert>
</mapper>